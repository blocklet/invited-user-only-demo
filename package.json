{
  "name": "invited-user-only-demo",
  "version": "1.1.1",
  "main": ".",
  "repository": "git@github.com:blocklet/invited-user-only-demo.git",
  "author": "Arcblock",
  "license": "MIT",
  "files": [
    "index.html"
  ],
  "scripts": {
    "clean": "rm -rf .blocklet",
    "bundle": "npm run clean && blocklet bundle --create-release",
    "deploy": "npm run bundle && blocklet deploy .blocklet/bundle"
  }
}
